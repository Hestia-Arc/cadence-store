// import { useCartStore } from "../features"

// export const Badge = () => {
//   const cart = useCartStore(state => state.cart )
//   return (
//     <div className='w-5 h-5 flex items-center justify-center bg-primary-800 rounded-3xl text-white text-[12px] font-bold'>{cart.length ? cart.length : 0}</div>
//   )
// }
