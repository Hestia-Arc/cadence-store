import { CardValue } from "../../components/CardValue";

export const Values = () => {
  return (
    <section className="h-[316px] flex gap-5 items-center px-40 py-12 bg-white">
      <CardValue />
      <CardValue />
      <CardValue />
      <CardValue />
    </section>
  );
};
